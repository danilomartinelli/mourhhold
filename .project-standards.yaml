# Standards Tracking
# Generated by /configure:all
standards_version: "2025.1"
project_type: "rust" # Bevy Top-Down 2D RPG (Cargo workspace)
last_configured: "2026-06-25T20:01:05Z"

components:
  pre-commit: "2025.1" # .pre-commit-config.yaml (+ commit-msg hook)
  release-please: "2025.1" # release-please-config.json + manifest, rust + cargo-workspace
  workflows: "2025.1" # .github/workflows: ci, release-please, docs
  docs: "2025.1" # rustdoc via docs.yml
  github-pages: "2025.1" # deployed by docs.yml
  tests: "2025.1" # cargo nextest (fallback cargo test)
  coverage: "2025.1" # cargo-llvm-cov (just coverage)
  linting: "2025.1" # clippy pedantic+nursery, -D warnings in CI
  formatting: "2025.1" # rustfmt.toml
  dead-code: "2025.1" # cargo-machete + unreachable_pub/unused lints
  editor: "2025.1" # .editorconfig + .vscode recommendations
  security: "2025.1" # cargo-deny (advisories/licenses/sources)
  makefile: "2025.1" # justfile used instead of Makefile

# Components intentionally not configured for this project type.
deviations:
  - component: dockerfile
    reason: "Desktop GPU game; not meaningfully containerized."
  - component: container
    reason: "Desktop GPU game; no container image target."
  - component: skaffold
    reason: "No Kubernetes; not applicable."
  - component: cache-busting
    reason: "Not applicable to a native Rust binary."
  - component: memory-profiling
    reason: "Python-specific (pytest-memray); N/A for Rust."
  - component: sentry
    reason: "Optional for Rust; defer until there is a build to instrument."
